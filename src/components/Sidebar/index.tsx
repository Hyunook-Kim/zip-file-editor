import styled from "styled-components";

const Sidebar = () => {
  return (
    <SidebarContainer>
      <h3>File Tree</h3>
    </SidebarContainer>
  );
};

const SidebarContainer = styled.div`
  padding: 10px;
`;

export default Sidebar;
